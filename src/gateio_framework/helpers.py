# src/gateio_framework/helpers.py

import time
from datetime import datetime, timezone, timedelta
from decimal import Decimal

class TradingWebSocketGateHelper:
    """Helper class to build request payloads."""
    # ... (將您原本的 TradingWebSocketGateHelper 類別的程式碼貼到這裡) ...

class SpotOrderResponseJsonResult:
    """A data class to parse and access spot order results."""
    # ... (將您原本的 SpotOrderResponseJsonResult 類別的程式碼貼到這裡) ...